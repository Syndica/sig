const shared = @import("shared");

pub const DEFAULT_LEADER_SCHEDULE_SLOT_OFFSET =
    shared.core.epoch_schedule.DEFAULT_LEADER_SCHEDULE_SLOT_OFFSET;
pub const MINIMUM_SLOTS_PER_EPOCH = shared.core.epoch_schedule.MINIMUM_SLOTS_PER_EPOCH;
pub const EpochSchedule = shared.core.EpochSchedule;
