const shared = @import("shared");

pub const Epoch = shared.core.Epoch;
pub const Slot = shared.core.Slot;
pub const UnixTimestamp = shared.core.UnixTimestamp;
pub const DEFAULT_TICKS_PER_SECOND = shared.core.time.DEFAULT_TICKS_PER_SECOND;
pub const DEFAULT_TICKS_PER_SLOT = shared.core.time.DEFAULT_TICKS_PER_SLOT;
pub const SECONDS_PER_DAY = shared.core.time.SECONDS_PER_DAY;
pub const TICKS_PER_DAY = shared.core.time.TICKS_PER_DAY;
pub const DEFAULT_SLOTS_PER_EPOCH = shared.core.time.DEFAULT_SLOTS_PER_EPOCH;
