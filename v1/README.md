# Sig v1

This is the original implementation of Sig. It implements the full solana protocol except for block production.

Sig v1 is in maintenance mode with only critical bug fixes being worked on. New development happens in `../v2/`.
